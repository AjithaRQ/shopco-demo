revolte:
  version: '1.0'
  project: Gemini
  appName: gemapp
  config:
    codeReview:
      summarize: true
      suggestFixes: false
      commentStyle: Verbose
    codeQuality:
      codeStyle:
        rules:
          javascript/noUndef: warn
          javascript/noUnusedVars: warn
  env:
    staging:
      branch: staging
      codeReview: true
      codeQuality: true
      services:
        rid_1:
          type: WEB_SERVICE
          name: web-service
          entryPoint: index.js
          properties:
            health: /api/health
            port: 8080
            variables:
              BASE_URL: /api/v1
              DATABASE_URL: '#{db.dbConnectionString}'
              PORT: 8080
          specifications:
            cpu: 2
            memory: 1
            storage: 30
        rid_2:
          type: DATABASE
          name: db
          specifications:
            cpu: 2
            memory: 1
            storage: 20
            engine: postgres
    qa:
      branch: staging
      codeReview: true
      codeQuality: true
      services:
        rid_6:
          type: WEB_SERVICE
          name: web-service
          entryPoint: index.js
          properties:
            health: /api/health
            port: 8080
            variables:
              BASE_URL: /api/v1
              DATABASE_URL: '#{db.dbConnectionString}'
              PORT: '8080'
          insights:
            cpu:
              - cpu_time_idle
            swap:
              - swap_used
            mem:
              - mem_total
            disk:
              - disk_total
          specifications:
            cpu: 2
            memory: 1
            storage: 30
        rid_7:
          type: DATABASE
          name: db
          specifications:
            cpu: 2
            memory: 1
            storage: 20
            engine: postgres
    production:
      branch: main
      codeReview: true
      codeQuality: true
      services:
        rid_1:
          type: WEB_SERVICE
          name: web-service
          entryPoint: index.js
          properties:
            health: /api/health
            port: 8080
            variables:
              BASE_URL: /api/v1
              DATABASE_URL: '#{db.dbConnectionString}'
              PORT: '#{db.dbPort}'
          specifications:
            cpu: 2
            memory: 1
            storage: 30
            autoScaling:
              replica:
                min: 1
                max: 3
        rid_2:
          type: DATABASE
          name: db
          specifications:
            cpu: 2
            memory: 1
            storage: 20
            engine: postgres
    preview:
      codeReview: false
      codeQuality: false
      services:
        rid_1:
          type: WEB_SERVICE
          name: web_service
          entryPoint: index.js
          properties:
            health: /api/health
            port: 8080
            variables:
              BASE_URL: /api/v1
              DATABASE_URL: '#{db.dbUrl}'
              DATABASE_PORT: '#{db.dbPort}'
              DATABASE_USERNAME: '#{db.dbUsername}'
              DATABASE_PASSWORD: '#{db.dbPassword}'
              DATABASE_NAME: '#{db.dbName}'
          specifications:
            cpu: 2
            memory: 1
            storage: 30
        rid_2:
          type: DATABASE
          name: db
          specifications:
            cpu: 2
            memory: 1
            storage: 30
            engine: postgres
